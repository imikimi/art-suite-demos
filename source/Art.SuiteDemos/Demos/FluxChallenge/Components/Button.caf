import &StandardImport

class Button extends PointerActionsMixin Component

  render: ->
    enabled = !!@props.action

    Element
      margin: &StyleProps.basePadding
      on: @pointerHandlers
      opacity: if enabled then 1 else .5
      animators: opacity: true
      enabled && cursor: :pointer

      RectangleElement
        color: &StyleProps.getButtonBackgoundColor {enabled, @pointerIsDown, @hover}
        radius: 1
        animators: color: true
        FillElement()
        OutlineElement &StyleProps.buttonOutlineStyle

      TextElement
        &StyleProps.buttonTextStyle
        text: if @props.up then :⥣ else :⥥
