import &StandardImport

class TheNorthernPeople extends Component

  render: ->
    {text, seed, options = {}} = @props

    random = &createRandomGenerator seed = 1

    lines = getTextLines {} text, random, aspectRatio: 2

